import React from "react";
import Eraser from "./CanvasComponents/Eraser";

const Options = () => {
  return (
    <React.Fragment>
      <Eraser />
    </React.Fragment>
  );
};

export default Options;
